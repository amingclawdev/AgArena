# Server only. Copy to .env, which is ignored by git. Never use a VITE_ prefix.
X_BEARER_TOKEN=
# Enable only after approving X API usage for the manual fetch.
# Up to 1 user lookup + 5 post reads per server start. No retries/polling.
X_READS_ENABLED=false
